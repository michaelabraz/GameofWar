var expect = chai.expect 

describe('gameOfWarFunctions', function() {
    describe('#makeDeck', function() {
        it('should have 52 cards', function() {
            var testDeck = makeDeck();
            expect(x).length.to.equal(52);
        })
    })
})