import { FullCardDeck } from "./cardDeck";
import { expect } from "chai";

describe('gameOfWarFunctions', function() {
    describe('#makeDeck', function() {
        it('should have 52 cards', function() {
            // var fullDeck = new FullCardDeck;
            // console.log('test is trying to run now');
            // var testDeck = fullDeck.makeDeck();
            // expect(testDeck).length.to.equal(52);
        })
    })
})